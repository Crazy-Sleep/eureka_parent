spring:
  cloud:
    config:
      uri: http://localhost:5001  # 服务启动后，去找5555服务
      name: microservice-config  # 从github上读取的资源名称，不要加 .yml后缀
      label: master # master分支
      profile: dev  # dev配置

#spring:
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        serviceId: bs-config #配置中心的名称
#      name: microservice-config  # 从github上读取的资源名称，不要加 .yml后缀
#      label: master # master分支
#      profile: dev  # dev配置
