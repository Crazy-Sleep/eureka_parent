server:
  port: 5001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
  instance:
    prefer-ip-address: true
spring:
  application:
    name: bs-config  # 配置中心的名称
  cloud:
    config:
      server:
        git:
          #gitlib地址
          uri: https://github.com/Crazy-Sleep/bs_config.git
          #gitlib的用户名 和 密码  如果是public 项目，则不需要写
          username:
          password:
